<!DOCTYPE html>
<html lang="en">
<%- include('../partials/header') %>

    <body style="font-family: Arial, sans-serif; background-color: #f4f6f9; margin: 0;">

        <%- include('../partials/nav') %>

            <header>
                <h1 style="
            text-align: center;
            margin-top: 30px;
            margin-bottom: 20px;
            color: #333;
        ">
                    <%= editing ? "Edit" : "Register" %> Your Home on AirBnb 🏠
                </h1>
            </header>

            <main>

                <form action="/host/<%= editing ? 'edit-home' : 'add-home' %>" method="POST" style="
                width: 35%;
                background-color: white;
                border-radius: 20px;
                padding: 30px;
                display: flex;
                flex-direction: column;
                gap: 15px;
                margin: 30px auto;
                box-shadow: 0 8px 20px rgba(0,0,0,0.1);
            ">

                    <!-- Hidden ID (Only for Edit Mode) -->
                    <% if (editing) { %>
                        <input type="hidden" name="id" value="<%= home.id %>" />
                        <% } %>

                            <!-- House Name -->
                            <label style="font-weight: bold;">House Name</label>
                            <input type="text" name="houseName" placeholder="Enter house name"
                                value="<%= editing ? home.houseName : '' %>" style="
                    padding: 10px;
                    border-radius: 8px;
                    border: 1px solid #ccc;
                    outline: none;
                " required />

                            <!-- Price -->
                            <label style="font-weight: bold;">Price per Night</label>
                            <input type="number" name="price" placeholder="Enter price per night"
                                value="<%= editing ? home.price : '' %>" style="
                    padding: 10px;
                    border-radius: 8px;
                    border: 1px solid #ccc;
                    outline: none;
                " required />

                            <!-- Location -->
                            <label style="font-weight: bold;">Location</label>
                            <input type="text" name="location" placeholder="Enter location"
                                value="<%= editing ? home.location : '' %>" style="
                    padding: 10px;
                    border-radius: 8px;
                    border: 1px solid #ccc;
                    outline: none;
                " required />

                            <!-- Image -->
                            <label style="font-weight: bold;">Image URL</label>
                            <input type="url" name="img" placeholder="Enter image URL"
                                value="<%= editing ? home.img : '' %>" style="
                    padding: 10px;
                    border-radius: 8px;
                    border: 1px solid #ccc;
                    outline: none;
                " required />

                            <!-- Submit Button -->
                            <input type="submit" value="<%= editing ? 'Update' : 'Add' %> Home" style="
                    margin-top: 10px;
                    padding: 12px;
                    border-radius: 10px;
                    border: none;
                    background-color: #ff385c;
                    color: white;
                    font-size: 16px;
                    cursor: pointer;
                " />

                </form>

            </main>

    </body>

</html>